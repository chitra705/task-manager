<!DOCTYPE html>
<html>
<head>
  <title>Edit Lead</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    .cnt {
      border: 2px solid gray;
      margin-bottom: 40px;
      margin-top: 40px;
      border-radius: 10px;
      padding: 50px;
    }
    h3 {
      margin-top: 20px;
      text-align: center;
      font-weight: bolder;
    }
  </style>
</head>
<body>
  <h3>Edit Lead</h3>

  <div class="container cnt">
    <%= form_with(model: @leads, local: true, url: leads_path(@leads), method: :patch, html: { class: "form-horizontal" }) do |form| %>
      <% if @leads.errors.any? %>
        <div id="error_explanation" class="alert alert-danger">
          <h4><%= pluralize(@leads.errors.count, "error") %> prohibited this lead from being saved:</h4>
          <ul>
            <% @leads.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group row">
        <%= form.label :status, class: "col-sm-2 col-form-label" %>
        <div class="col-sm-10">
          <%= form.select :status, Lead.statuses.keys.map { |status| [status.titleize, status] }, { include_blank: 'Select Status' }, class: "form-control" %>
        </div>
      </div>

      <div class="form-group row">
        <%= form.label :category, class: "col-sm-2 col-form-label" %>
        <div class="col-sm-10">
          <%= form.text_field :category, class: "form-control" %>
        </div>
      </div>

      <div class="form-group row">
        <%= form.label :category_name, class: "col-sm-2 col-form-label" %>
        <div class="col-sm-10">
          <%= form.text_field :category_name, class: "form-control" %>
        </div>
      </div>

      <div class="form-group row">
        <%= form.label :business_name, class: "col-sm-2 col-form-label" %>
        <div class="col-sm-10">
          <%= form.text_field :business_name, class: "form-control" %>
        </div>
      </div>

      <div class="form-group row">
        <%= form.label :address, class: "col-sm-2 col-form-label" %>
        <div class="col-sm-10">
          <%= form.text_field :address, class: "form-control" %>
        </div>
      </div>

      <div class="form-group row">
        <%= form.label :phone, class: "col-sm-2 col-form-label" %>
        <div class="col-sm-10">
          <%= form.telephone_field :phone, class: "form-control" %>
        </div>
      </div>

      <div class="form-group row">
        <div class="col-sm-10 offset-sm-2">
          <%= form.submit "Update Lead", class: "btn btn-primary" %>
          <%= link_to 'Cancel', leads_path, class: "btn btn-secondary ml-2" %>
        </div>
      </div>
    <% end %>
  </div>
</body>
</html>
